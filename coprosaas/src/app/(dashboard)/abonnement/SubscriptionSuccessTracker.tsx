// L'événement `purchase` est envoyé côté serveur via GA4 Measurement Protocol
// dans /api/stripe/webhook (sendGa4PurchaseEvent) — pas de tracking client-side.
// Ce fichier est conservé vide pour éviter des erreurs d'import éventuels.
export default function SubscriptionSuccessTracker() {
  return null;
}
